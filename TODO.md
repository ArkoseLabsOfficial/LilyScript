# TODO:

- [x] Class Based Scripting System
- - [ ] Fix setting parent object value with only using variable name
- - [ ] make `importScript()` per script instead of importing running script classes into script
- - [ ] `importScript()` directly in SScript
- - [ ] `importScript("path/file.class");` support
- - [ ] `importScript("path/*");` support
- [ ] Allow Json access without using `scripting` toggle
- [ ] Allow full Map usage